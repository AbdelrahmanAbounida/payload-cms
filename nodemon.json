{
  "$schema": "https://json.schemastore.org/nodemon.json",
  "watch": ["server.ts", "payload/**/*.ts"],
  "exec": "ts-node --project tsconfig.server.json server.ts -- -I",
  "ext": "js ts",
  "ignore": ["app"],
  "stdin": false
}
